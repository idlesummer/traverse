import { defineConfig } from 'oxfmt'

export default defineConfig({
  ignorePatterns: [],

  // Formatting
  trailingComma: 'all',
  bracketSpacing: true,
  singleQuote: true,
  semi: false,
  endOfLine: 'lf',
  insertFinalNewline: true,
})
